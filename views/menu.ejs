<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu</title>
</head>

<body>
    <%- include('./partials/nav') %>
        <h1>Full Menu</h1>
        <ul>
            <%menu.forEach((dish)=> { %>
                <li> Name: <%=dish.name %> - $<%=dish.price %>
                            <%=dish.rating===5 ? "🌟" : " " %><br />
                                Rating: <%=dish.rating %> stars<br />
                                    Description: <%=dish.details %>
                </li>
                <% }) %>
        </ul>
</body>

</html>